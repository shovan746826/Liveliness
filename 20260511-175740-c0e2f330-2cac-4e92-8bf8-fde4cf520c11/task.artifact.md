# Task Management

- [x] Extract hardcoded strings from `KycController` (Refactored `IntroScreen` and created `AppStrings`)
- [x] Fix `tflite_flutter` compilation error (`UnmodifiableUint8ListView`)
- [x] Fix `styles.xml` "processing instruction" error
- [/] Fix `CameraImage` format resolution error in `KycController`
- [ ] Verify `KycController` logic after fixes
- [ ] Move hardcoded strings in `KycController` to `AppStrings` (Remaining strings)
